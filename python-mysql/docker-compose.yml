version: "3.9"
services:
  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: testdb
    volumes:
      - db_data:/var/lib/mysql
  web:
    build: ./app
    ports:
      - "5000:5000"
    depends_on:
      - db
volumes:
  db_data: